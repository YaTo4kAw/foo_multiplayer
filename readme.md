server side:
launch -> type your host and port -> be happy

client side:
launch -> type your domain, port and playlist number to sync with -> be happy

to change config after first executing, use config.toml file